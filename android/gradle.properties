org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# Support for 16 KB page sizes
android.experimental.elf-binding-support=true

# CRITICAL: Flutter native library optimization for Galaxy A01 Core fix
android.enableR8.fullMode=false

# Ensure all architectures are built and included
# (Removed injected build ABI to prevent 'test-only' APK error)
